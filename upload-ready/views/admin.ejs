<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>管理員頁面 - 自動掃碼簽到系統</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .sidebar {
      min-height: 100vh;
      background-color: #343a40;
      color: #fff;
      padding-top: 20px;
    }
    .sidebar-link {
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      padding: 10px 15px;
      display: block;
      transition: all 0.3s;
    }
    .sidebar-link:hover {
      color: #fff;
      background-color: rgba(255, 255, 255, 0.1);
    }
    .sidebar-link.active {
      color: #fff;
      background-color: rgba(255, 255, 255, 0.2);
    }
    .main-content {
      padding: 20px;
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <!-- 側邊欄 -->
      <div class="col-md-3 col-lg-2 d-md-block sidebar collapse">
        <div class="text-center mb-4">
          <h4>掃碼簽到系統</h4>
          <p>管理後台</p>
        </div>
        <hr>
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="sidebar-link" href="/admin/dashboard">
              <i class="bi bi-speedometer2 me-2"></i> 儀表板
            </a>
          </li>
          <li class="nav-item">
            <a class="sidebar-link active" href="/admin">
              <i class="bi bi-calendar-event me-2"></i> 活動管理
            </a>
          </li>

          <li class="nav-item">
            <a class="sidebar-link" href="/admin/checkins">
              <i class="bi bi-check2-square me-2"></i> 簽到記錄
            </a>
          </li>
          
          <% if (admin && admin.role === 'super') { %>
          <li class="nav-item">
            <a class="sidebar-link" href="/admin/admins">
              <i class="bi bi-shield-lock me-2"></i> 管理員帳號
            </a>
          </li>
          <li class="nav-item">
            <a class="sidebar-link" href="/admin/pending">
              <i class="bi bi-hourglass-split me-2"></i> 待審核申請
            </a>
          </li>
          <% } %>
          
          <li class="nav-item mt-5">
            <a class="sidebar-link text-danger" href="/admin/logout">
              <i class="bi bi-box-arrow-right me-2"></i> 登出
            </a>
          </li>
        </ul>
      </div>
      
      <!-- 主要內容 -->
      <div class="col-md-9 col-lg-10 main-content">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h2>活動管理</h2>
          <div>
            <span class="me-2">歡迎，<%= admin.name %></span>
            <span class="badge <%= admin.role === 'super' ? 'bg-danger' : 'bg-primary' %>">
              <%= admin.role === 'super' ? '超級管理者' : '管理者' %>
            </span>
          </div>
        </div>

    <div class="row mb-4">
      <div class="col">
        <div class="card shadow">
          <div class="card-header bg-primary text-white">
            <h3 class="card-title mb-0">創建新活動</h3>
          </div>
          <div class="card-body">
            <form action="/events" method="POST">
              <div class="mb-3">
                <label for="title" class="form-label">活動名稱</label>
                <input type="text" class="form-control" id="title" name="title" required>
              </div>
              <div class="mb-3">
                <label for="description" class="form-label">活動描述</label>
                <textarea class="form-control" id="description" name="description" rows="3"></textarea>
              </div>
              <div class="row">
                <div class="col-md-6 mb-3">
                  <label for="start_time" class="form-label">開始時間</label>
                  <input type="datetime-local" class="form-control" id="start_time" name="start_time" required>
                </div>
                <div class="col-md-6 mb-3">
                  <label for="end_time" class="form-label">結束時間</label>
                  <input type="datetime-local" class="form-control" id="end_time" name="end_time" required>
                </div>
              </div>
              <button type="submit" class="btn btn-primary">創建活動</button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col">
        <div class="card shadow">
          <div class="card-header bg-secondary text-white">
            <h3 class="card-title mb-0">活動列表</h3>
          </div>
          <div class="card-body">
            <% if (events && events.length > 0) { %>
              <div class="table-responsive">
                <table class="table table-striped table-hover">
                  <thead>
                    <tr>
                      <th>活動名稱</th>
                      <th>開始時間</th>
                      <th>結束時間</th>
                      <th>操作</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% events.forEach(event => { %>
                      <tr>
                        <td><%= event.title %></td>
                        <td><%= new Date(event.start_time).toLocaleString() %></td>
                        <td><%= new Date(event.end_time).toLocaleString() %></td>
                        <td>
                          <a href="/events/<%= event.id %>/qrcode" class="btn btn-sm btn-primary">
                            <i class="bi bi-qr-code"></i> 查看QR碼
                          </a>
                          <a href="/events/<%= event.id %>/checkins" class="btn btn-sm btn-info">
                            <i class="bi bi-list-check"></i> 簽到記錄
                          </a>
                          <form action="/events/<%= event.id %>/update-qrcode" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-warning">
                              <i class="bi bi-arrow-clockwise"></i> 更新QR碼
                            </button>
                          </form>
                        </td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <div class="alert alert-info">
                目前沒有活動，請創建新活動。
              </div>
            <% } %>
          </div>
        </div>
      </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>