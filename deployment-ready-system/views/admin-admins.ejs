<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理員帳號管理 - 掃碼簽到系統</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .sidebar {
            min-height: 100vh;
            background-color: #343a40;
            color: #fff;
            padding-top: 20px;
        }
        .sidebar-link {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            padding: 10px 15px;
            display: block;
            transition: all 0.3s;
        }
        .sidebar-link:hover {
            color: #fff;
            background-color: rgba(255, 255, 255, 0.1);
        }
        .sidebar-link.active {
            color: #fff;
            background-color: rgba(255, 255, 255, 0.2);
        }
        .main-content {
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- 側邊欄 -->
            <div class="col-md-3 col-lg-2 d-md-block sidebar collapse">
                <div class="text-center mb-4">
                    <h4>掃碼簽到系統</h4>
                    <p>管理後台</p>
                </div>
                <hr>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="sidebar-link" href="/admin/dashboard">
                            <i class="bi bi-speedometer2 me-2"></i> 儀表板
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="sidebar-link" href="/admin">
                            <i class="bi bi-calendar-event me-2"></i> 活動管理
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="sidebar-link" href="/admin/checkins">
                            <i class="bi bi-check2-square me-2"></i> 簽到記錄
                        </a>
                    </li>
                    
                    <% if (admin && admin.role === 'super') { %>
                    <li class="nav-item">
                        <a class="sidebar-link active" href="/admin/admins">
                            <i class="bi bi-shield-lock me-2"></i> 管理員帳號
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="sidebar-link" href="/admin/pending">
                            <i class="bi bi-hourglass-split me-2"></i> 待審核申請
                        </a>
                    </li>
                    <% } %>
                    
                    <li class="nav-item mt-5">
                        <a class="sidebar-link text-danger" href="/admin/logout">
                            <i class="bi bi-box-arrow-right me-2"></i> 登出
                        </a>
                    </li>
                </ul>
            </div>
            
            <!-- 主要內容 -->
            <div class="col-md-9 col-lg-10 main-content">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2>管理員帳號管理</h2>
                    <div>
                        <span class="me-2">歡迎，<%= admin.name %></span>
                        <span class="badge bg-danger">超級管理者</span>
                    </div>
                </div>
                
                <% if(typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
                    <div class="alert alert-success" role="alert">
                        <%= success_msg %>
                    </div>
                <% } %>
                
                <% if(typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                    <div class="alert alert-danger" role="alert">
                        <%= error_msg %>
                    </div>
                <% } %>
                
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <span>管理員帳號列表</span>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>用戶名</th>
                                        <th>姓名</th>
                                        <th>電子郵件</th>
                                        <th>角色</th>
                                        <th>狀態</th>
                                        <th>創建時間</th>
                                        <th>操作</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% admins.forEach(adminUser => { %>
                                        <tr>
                                            <td><%= adminUser.username %></td>
                                            <td><%= adminUser.name %></td>
                                            <td><%= adminUser.email %></td>
                                            <td>
                                                <span class="badge <%= adminUser.role === 'super' ? 'bg-danger' : 'bg-primary' %>">
                                                    <%= adminUser.role === 'super' ? '超級管理者' : '管理者' %>
                                                </span>
                                            </td>
                                            <td>
                                                <% if (adminUser.status === 'approved') { %>
                                                    <span class="badge bg-success">已核准</span>
                                                <% } else if (adminUser.status === 'pending') { %>
                                                    <span class="badge bg-warning text-dark">待審核</span>
                                                <% } else if (adminUser.status === 'disabled') { %>
                                                    <span class="badge bg-secondary">已停用</span>
                                                <% } %>
                                            </td>
                                            <td><%= adminUser.created_at %></td>
                                            <td>
                                                <% if (adminUser.id !== admin.id && adminUser.role !== 'super') { %>
                                                    <% if (adminUser.status === 'approved') { %>
                                                        <form action="/admin/disable/<%= adminUser.id %>" method="POST" class="d-inline">
                                                            <button type="submit" class="btn btn-sm btn-warning">停用</button>
                                                        </form>
                                                    <% } else if (adminUser.status === 'disabled') { %>
                                                        <form action="/admin/enable/<%= adminUser.id %>" method="POST" class="d-inline">
                                                            <button type="submit" class="btn btn-sm btn-success">啟用</button>
                                                        </form>
                                                    <% } %>
                                                    <form action="/admin/delete/<%= adminUser.id %>" method="POST" class="d-inline" onsubmit="return confirm('確定要刪除此管理員帳號嗎？此操作無法撤銷。')">
                                                        <button type="submit" class="btn btn-sm btn-danger">刪除</button>
                                                    </form>
                                                <% } else { %>
                                                    <span class="text-muted">無法操作</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>